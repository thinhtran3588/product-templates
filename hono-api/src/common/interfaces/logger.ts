import type { Logger as PinoLogger } from 'pino';

/**
 * Logger interface for application-wide logging
 * Based on pino's Logger for high-performance logging and consistency
 */
export type Logger = PinoLogger;
