import { describe, expect, it } from 'vitest';

describe('common/infrastructure/schema', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/infrastructure/schema').toBe('common/infrastructure/schema');
  });
});
