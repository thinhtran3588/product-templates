import { describe, expect, it } from 'vitest';

describe('common/constants', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/constants').toBe('common/constants');
  });
});
