import { describe, expect, it } from 'vitest';

describe('common/utils/success-response-schema', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/utils/success-response-schema').toBe(
      'common/utils/success-response-schema'
    );
  });
});
