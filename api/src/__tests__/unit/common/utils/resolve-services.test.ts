import { describe, expect, it } from 'vitest';

describe('common/utils/resolve-services', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/utils/resolve-services').toBe(
      'common/utils/resolve-services'
    );
  });
});
