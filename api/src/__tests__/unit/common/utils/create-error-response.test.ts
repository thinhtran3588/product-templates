import { describe, expect, it } from 'vitest';

describe('common/utils/create-error-response', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/utils/create-error-response').toBe(
      'common/utils/create-error-response'
    );
  });
});
