import { describe, expect, it } from 'vitest';

describe('common/utils/full-text-search', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/utils/full-text-search').toBe(
      'common/utils/full-text-search'
    );
  });
});
