import { describe, expect, it } from 'vitest';

describe('common/utils/to-application-context', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/utils/to-application-context').toBe(
      'common/utils/to-application-context'
    );
  });
});
