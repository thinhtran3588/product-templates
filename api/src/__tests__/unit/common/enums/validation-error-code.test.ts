import { describe, expect, it } from 'vitest';

describe('common/enums/validation-error-code', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/enums/validation-error-code').toBe(
      'common/enums/validation-error-code'
    );
  });
});
