import { describe, expect, it } from 'vitest';

describe('common/enums/authorization-exception-code', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/enums/authorization-exception-code').toBe(
      'common/enums/authorization-exception-code'
    );
  });
});
