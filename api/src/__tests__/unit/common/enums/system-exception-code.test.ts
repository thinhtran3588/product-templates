import { describe, expect, it } from 'vitest';

describe('common/enums/system-exception-code', () => {
  it('has a matching unit test scaffold', () => {
    expect('common/enums/system-exception-code').toBe(
      'common/enums/system-exception-code'
    );
  });
});
