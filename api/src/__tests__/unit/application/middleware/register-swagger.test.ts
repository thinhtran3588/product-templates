import { describe, expect, it } from 'vitest';

describe('application/middleware/register-swagger', () => {
  it('has a matching unit test scaffold', () => {
    expect('application/middleware/register-swagger').toBe(
      'application/middleware/register-swagger'
    );
  });
});
