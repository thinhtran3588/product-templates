import { describe, expect, it } from 'vitest';

describe('application/middleware/register-error-handler', () => {
  it('has a matching unit test scaffold', () => {
    expect('application/middleware/register-error-handler').toBe(
      'application/middleware/register-error-handler'
    );
  });
});
