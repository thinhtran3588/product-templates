import { describe, expect, it } from 'vitest';

describe('modules/auth/domain/enums/user-status', () => {
  it('has a matching unit test scaffold', () => {
    expect('modules/auth/domain/enums/user-status').toBe(
      'modules/auth/domain/enums/user-status'
    );
  });
});
