import { describe, expect, it } from 'vitest';

describe('modules/auth/domain/enums/user-event-type', () => {
  it('has a matching unit test scaffold', () => {
    expect('modules/auth/domain/enums/user-event-type').toBe(
      'modules/auth/domain/enums/user-event-type'
    );
  });
});
