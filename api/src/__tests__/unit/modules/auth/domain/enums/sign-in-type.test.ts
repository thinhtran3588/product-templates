import { describe, expect, it } from 'vitest';

describe('modules/auth/domain/enums/sign-in-type', () => {
  it('has a matching unit test scaffold', () => {
    expect('modules/auth/domain/enums/sign-in-type').toBe(
      'modules/auth/domain/enums/sign-in-type'
    );
  });
});
