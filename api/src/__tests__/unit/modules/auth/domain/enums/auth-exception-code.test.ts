import { describe, expect, it } from 'vitest';

describe('modules/auth/domain/enums/auth-exception-code', () => {
  it('has a matching unit test scaffold', () => {
    expect('modules/auth/domain/enums/auth-exception-code').toBe(
      'modules/auth/domain/enums/auth-exception-code'
    );
  });
});
