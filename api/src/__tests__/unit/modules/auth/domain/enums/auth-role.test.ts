import { describe, expect, it } from 'vitest';

describe('modules/auth/domain/enums/auth-role', () => {
  it('has a matching unit test scaffold', () => {
    expect('modules/auth/domain/enums/auth-role').toBe(
      'modules/auth/domain/enums/auth-role'
    );
  });
});
