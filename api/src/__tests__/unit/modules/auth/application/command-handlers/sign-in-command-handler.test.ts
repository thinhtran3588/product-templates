import { describe, expect, it } from 'vitest';

describe('modules/auth/application/command-handlers/sign-in-command-handler', () => {
  it('has a matching unit test scaffold', () => {
    expect(
      'modules/auth/application/command-handlers/sign-in-command-handler'
    ).toBe('modules/auth/application/command-handlers/sign-in-command-handler');
  });
});
