import { describe, expect, it } from 'vitest';

describe('modules/auth/application/command-handlers/register-command-handler', () => {
  it('has a matching unit test scaffold', () => {
    expect(
      'modules/auth/application/command-handlers/register-command-handler'
    ).toBe(
      'modules/auth/application/command-handlers/register-command-handler'
    );
  });
});
