import { describe, expect, it } from 'vitest';

describe('modules/auth/application/command-handlers/delete-user-group-command-handler', () => {
  it('has a matching unit test scaffold', () => {
    expect(
      'modules/auth/application/command-handlers/delete-user-group-command-handler'
    ).toBe(
      'modules/auth/application/command-handlers/delete-user-group-command-handler'
    );
  });
});
