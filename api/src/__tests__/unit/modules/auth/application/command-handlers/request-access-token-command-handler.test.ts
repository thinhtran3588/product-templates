import { describe, expect, it } from 'vitest';

describe('modules/auth/application/command-handlers/request-access-token-command-handler', () => {
  it('has a matching unit test scaffold', () => {
    expect(
      'modules/auth/application/command-handlers/request-access-token-command-handler'
    ).toBe(
      'modules/auth/application/command-handlers/request-access-token-command-handler'
    );
  });
});
