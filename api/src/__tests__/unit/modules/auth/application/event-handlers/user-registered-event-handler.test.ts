import { describe, expect, it } from 'vitest';

describe('modules/auth/application/event-handlers/user-registered-event-handler', () => {
  it('has a matching unit test scaffold', () => {
    expect(
      'modules/auth/application/event-handlers/user-registered-event-handler'
    ).toBe(
      'modules/auth/application/event-handlers/user-registered-event-handler'
    );
  });
});
