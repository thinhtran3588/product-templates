import { describe, expect, it } from 'vitest';

describe('modules/auth/adapters/user-group-adapter', () => {
  it('has a matching unit test scaffold', () => {
    expect('modules/auth/adapters/user-group-adapter').toBe(
      'modules/auth/adapters/user-group-adapter'
    );
  });
});
