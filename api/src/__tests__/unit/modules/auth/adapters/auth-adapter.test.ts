import { describe, expect, it } from 'vitest';

describe('modules/auth/adapters/auth-adapter', () => {
  it('has a matching unit test scaffold', () => {
    expect('modules/auth/adapters/auth-adapter').toBe(
      'modules/auth/adapters/auth-adapter'
    );
  });
});
