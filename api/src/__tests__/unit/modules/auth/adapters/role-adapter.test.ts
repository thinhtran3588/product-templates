import { describe, expect, it } from 'vitest';

describe('modules/auth/adapters/role-adapter', () => {
  it('has a matching unit test scaffold', () => {
    expect('modules/auth/adapters/role-adapter').toBe(
      'modules/auth/adapters/role-adapter'
    );
  });
});
