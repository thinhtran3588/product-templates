import { describe, expect, it } from 'vitest';

describe('modules/auth/infrastructure/repositories/user-group-repository-impl', () => {
  it('has a matching unit test scaffold', () => {
    expect(
      'modules/auth/infrastructure/repositories/user-group-repository-impl'
    ).toBe(
      'modules/auth/infrastructure/repositories/user-group-repository-impl'
    );
  });
});
