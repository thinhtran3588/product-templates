import { describe, expect, it } from 'vitest';

describe('modules/auth/infrastructure/repositories/role-repository-impl', () => {
  it('has a matching unit test scaffold', () => {
    expect(
      'modules/auth/infrastructure/repositories/role-repository-impl'
    ).toBe('modules/auth/infrastructure/repositories/role-repository-impl');
  });
});
