import { describe, expect, it } from 'vitest';

describe('modules/auth/infrastructure/services/external-authentication-service-impl', () => {
  it('has a matching unit test scaffold', () => {
    expect(
      'modules/auth/infrastructure/services/external-authentication-service-impl'
    ).toBe(
      'modules/auth/infrastructure/services/external-authentication-service-impl'
    );
  });
});
