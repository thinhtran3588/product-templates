import { describe, expect, it } from 'vitest';

describe('modules/auth/infrastructure/schema', () => {
  it('has a matching unit test scaffold', () => {
    expect('modules/auth/infrastructure/schema').toBe(
      'modules/auth/infrastructure/schema'
    );
  });
});
