# Copy to .env.development and .env.production, then fill in values.
# Firebase config: compact JSON with apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId, measurementId.
NEXT_PUBLIC_FIREBASE_CONFIG=
